# In python you can use the abs() function to return an absolute value of any number

# Below we will be returning absolute values of the numbers in the list 
int_list = [-12, -34, -45, -56, -13]

# We will now then print 
print([abs(i) for i in int_list])

# Output
[12, 34, 45, 56, 13]